<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sheldon.springbootinit.mapper.ChartInfoMapper">

    <insert id="insertChartInfo" parameterType="string">
        ${insertSql}
    </insert>

    <delete id="deleteChartInfo">
        ${deleteSql}
    </delete>

    <select id="getChartInfoById" parameterType="string" resultType="map">
        ${querySql}
    </select>
</mapper>
